# Release Folder 📦

Contains approved dictionary entries merged into a single file: `dictionary.md`.

This is the final public version used in:

- GitHub Pages site
- RDF export (via `vocab/`)
- Archival tagging (e.g. v1.0, v2.1)

To update, run:  

```bash
python scripts/generate_release.py
```